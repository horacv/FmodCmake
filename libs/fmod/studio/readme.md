FMOD Studio API

Add the **inc** folder and platform specific subdirectories inside the **lib** folder

Example:

- `studio/`
  - `inc/`
  - `lib/`
    - `x64/`
    - `x86/`
    - `apple/`
    - `etc..`